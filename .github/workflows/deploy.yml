name: Deploy Laravel Backend to Simply.com

on:
  push:
    branches:
      - master  # eller din primære branch

jobs:
  deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.3.6'  # Sørg for at bruge korrekt version

    - name: Install Composer
      run: sudo apt-get install composer

    - name: Install Composer dependencies
      run: composer install --no-dev --prefer-dist --optimize-autoloader

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: ftps  # eller ftp, afhængig af hvad din server understøtter
        local-dir: ./  # Sti til dine lokale filer, f.eks. root af dit repository
        server-dir: /public_html/hiya/php/  # Sti på serveren hvor filerne skal uploades
        exclude: |
          **/.git*
          **/.github*
          **/node_modules/**
        timeout: 60000  # Øger timeout til 60 sekunder
